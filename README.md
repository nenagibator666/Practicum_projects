# Конвертер изображений #
Исходный код конвертера изображений, который поддерживает следующие форматы: `PPM`,` BMP`, `JPG/JPEG`. \
В файлах директории `ImgLib` находится библиотека, которая поддерживает запись и загрузку изображений определённого формата (`ppm_image`, `bmp_image`, `jpg_image`).
